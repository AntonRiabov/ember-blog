import Ember from 'ember';

export default Ember.Route.extend({
  //model: function(){
    //console.log( 'id of post : ' + params.post_id);
    //console.log('record by id : ' + this.store.findRecord('post', params.post_id));
    //return this.store.findRecord('post', params.post_id);
  //}
});
